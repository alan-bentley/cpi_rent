R code used to create the summary statistics and run the models, for the Tenancy Bond administrative data.

You won't be able to run the code without access to the microdata (which is quite rightly held under lock-and-key at MBIE).
